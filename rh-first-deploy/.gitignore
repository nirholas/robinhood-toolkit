# Foundry build output
out/
cache/
broadcast/

# Deploy artifact — contains address/tx of your live deploy; regenerated by the script
deploy.json

# Never commit keys (they live encrypted in ~/.foundry/keystores anyway)
*.key
.env
